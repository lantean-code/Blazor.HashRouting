# Blazor.HashRouting {{VERSION}}

## Summary

Release for the `{{MAJOR_MINOR_PATCH}}` line of `Blazor.HashRouting`.

The release improves the integration with browser navigation to ensure that quick successive navigations are relayed correctly to the Blazor app.

## Installation

```bash
dotnet add package Blazor.HashRouting --version {{NUGET_VERSION}}
```
