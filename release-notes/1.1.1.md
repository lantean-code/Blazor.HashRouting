# Blazor.HashRouting {{VERSION}}

## Summary

Patch release for the `{{MAJOR_MINOR_PATCH}}` line of `Blazor.HashRouting`.

This release fixes internal `forceLoad: true` navigation so hash-routed destinations perform a real document reload while preserving the expected canonical hash URL.

## Highlights

- Fixes `NavigationManager.NavigateTo(..., forceLoad: true)` for internal hash-routed destinations

## Installation

```bash
dotnet add package Blazor.HashRouting --version {{NUGET_VERSION}}
```
